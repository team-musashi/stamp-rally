import 'package:flutter/material.dart';

/// https://pub.dev/packages/flutter_osm_plugin
class FlutterOsmPluginPage extends StatelessWidget {
  const FlutterOsmPluginPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('flutter_osm_plugin')),
    );
  }
}
