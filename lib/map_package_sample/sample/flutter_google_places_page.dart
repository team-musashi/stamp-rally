import 'package:flutter/material.dart';

/// https://pub.dev/packages/flutter_google_places
class FlutterGooglePlacesPage extends StatelessWidget {
  const FlutterGooglePlacesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('flutter_google_places')),
    );
  }
}
