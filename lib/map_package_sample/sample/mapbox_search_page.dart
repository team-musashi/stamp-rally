import 'package:flutter/material.dart';

/// https://pub.dev/packages/mapbox_search
class MapBoxSearchPage extends StatelessWidget {
  const MapBoxSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('mapbox_search')),
    );
  }
}
