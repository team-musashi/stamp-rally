import 'package:flutter/material.dart';

/// スタンプラリー詳細画面
class StampRallyExplanationPage extends StatelessWidget {
  const StampRallyExplanationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('スタンプラリー詳細')),
    );
  }
}
