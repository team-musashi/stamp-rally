import 'package:flutter/material.dart';

/// 参加中画面
class EntryView extends StatelessWidget {
  const EntryView({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(some): 参加中ListView
    return const Center(child: Text('参加中'));
  }
}
