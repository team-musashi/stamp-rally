import 'package:flutter/material.dart';

/// 完了済画面
class CompleteView extends StatelessWidget {
  const CompleteView({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(some): 完了済ListView
    return const Center(child: Text('完了済'));
  }
}
